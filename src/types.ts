export enum Firmware {
  UEFI = "UEFI",
  BIOS = "BIOS",
}

export enum Processor {
  AMD = "AMD",
  INTEL = "Intel",
}

export enum Editor {
  VIM = "vim",
  NANO = "nano",
}

export enum FileSystem {
  EXT4 = "ext4",
  BTRFS = "btrfs",
}

export enum Kernel {
  LINUX = "linux",
  LINUX_LTS = "linux-lts",
  LINUX_HARDENED = "linux-hardened",
  LINUX_ZEN = "linux-zen",
}
